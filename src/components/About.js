// About Page;

const About = () => {
  return (
    <div>
      <h1>About Page</h1>
      <p key={'Topic'}>This About page is rendered by react-router-dom package.</p>
      <p key={'Details'}>React router Dom : createBrowserRoute is imported by name and takes a array of objects, where each object is a route.</p>
    </div>
  );
};

export default About;